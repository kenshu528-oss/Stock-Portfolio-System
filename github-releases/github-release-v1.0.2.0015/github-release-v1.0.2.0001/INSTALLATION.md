# 安裝指南 (Installation Guide)

## 📋 系統需求

- **Node.js**: 16.0 或更高版本
- **npm**: 7.0 或更高版本
- **瀏覽器**: Chrome, Firefox, Safari, Edge (最新版本)

## 🚀 快速開始

### 1. 下載專案
```bash
git clone https://github.com/kenshu528-oss/Stock-Portfolio-System.git
cd Stock-Portfolio-System
```

### 2. 安裝前端依賴
```bash
npm install
```

### 3. 安裝後端依賴
```bash
cd backend
npm install
cd ..
```

### 4. 啟動應用

#### 方法一：分別啟動
```bash
# 終端機 1 - 啟動後端
cd backend
npm start

# 終端機 2 - 啟動前端
npm run dev
```

#### 方法二：使用批次檔 (Windows)
```bash
# 雙擊執行 quick-deploy.bat
```

### 5. 開啟瀏覽器
前往 `http://localhost:5173` 查看應用程式

## 🔧 開發環境設定

### 前端開發
```bash
npm run dev          # 開發模式
npm run build        # 生產建置
npm run preview      # 預覽建置結果
npm run test         # 執行測試
```

### 後端開發
```bash
cd backend
npm start            # 生產模式
npm run dev          # 開發模式 (需安裝 nodemon)
```

## 🌐 API 設定

後端服務預設運行在 `http://localhost:3001`

### 支援的 API 端點
- `GET /api/stock/:symbol` - 獲取股票資訊
- `GET /api/health` - 健康檢查

### 環境變數 (可選)
在 `backend` 資料夾建立 `.env` 檔案：
```env
PORT=3001
NODE_ENV=production
```

## 📱 瀏覽器相容性

| 瀏覽器 | 最低版本 | 建議版本 |
|--------|----------|----------|
| Chrome | 88+ | 最新版 |
| Firefox | 85+ | 最新版 |
| Safari | 14+ | 最新版 |
| Edge | 88+ | 最新版 |

## 🐛 常見問題

### Q: 前端無法連接後端？
A: 確認後端服務運行在 port 3001，檢查防火牆設定

### Q: 股票搜尋無結果？
A: 確認網路連線正常，API 服務可能暫時無法使用

### Q: 建置失敗？
A: 檢查 Node.js 版本，清除 node_modules 重新安裝

```bash
rm -rf node_modules package-lock.json
npm install
```

## 📞 技術支援

如遇到安裝問題，請：
1. 檢查系統需求
2. 查看瀏覽器控制台錯誤
3. 透過 GitHub Issues 回報問題

---

**祝您使用愉快！** 🎉