# FinMind API 優先策略規則 (FinMind API Priority Rules)

## 🎯 核心原則：FinMind API 為台股資料首選

### 絕對要求的API調用順序
- ✅ **第一優先：FinMind API** - 台股專用，中文名稱，資料最準確
- ✅ **第二備用：台灣證交所API** - 中文名稱，官方資料
- ✅ **最後備用：Yahoo Finance API** - 英文名稱，國際通用

## 📊 API優先順序規範

### 1. FinMind API (首選)
```javascript
// 優先使用FinMind API
const stockData = await getFinMindStockPrice(symbol);
```
- **優勢**: 台股專用API，資料最準確
- **名稱**: 提供中文股票名稱
- **覆蓋**: 台股、ETF、債券等完整覆蓋
- **更新**: 即時資料更新
- **穩定性**: 專為台股設計，穩定可靠

### 2. 台灣證交所API (備用)
```javascript
// FinMind失敗時使用
const stockData = await getTWSEStockPrice(symbol);
```
- **優勢**: 官方資料來源
- **名稱**: 中文股票名稱
- **覆蓋**: 上市股票為主
- **限制**: 部分ETF和債券可能不支援

### 3. Yahoo Finance API (最後備用)
```javascript
// 前兩個API都失敗時使用
const stockData = await getYahooStockPrice(symbol);
```
- **優勢**: 國際通用，穩定性高
- **名稱**: 英文股票名稱
- **覆蓋**: 全球股票市場
- **缺點**: 中文用戶體驗較差

## 🚨 強制要求

### API調用必須遵循的規則
- ❌ **禁止跳過FinMind API**: 除非技術原因，必須優先嘗試FinMind
- ❌ **禁止直接使用Yahoo Finance**: 不能跳過中文名稱的API
- ❌ **禁止硬編碼股票名稱**: 必須使用API返回的真實資料
- ✅ **必須記錄API調用順序**: Console日誌要清楚顯示嘗試順序

### 錯誤處理規範
```javascript
// ✅ 正確的錯誤處理
try {
  stockData = await getFinMindStockPrice(symbol);
  console.log(`✅ FinMind成功獲取 ${symbol} 中文名稱股價資料`);
} catch (error) {
  console.log(`FinMind API 失敗: ${symbol}`, error.message);
  // 繼續嘗試下一個API
}
```

## 📋 實作檢查清單

### 每次修改API相關代碼必須確認
- [ ] FinMind API是否為第一優先？
- [ ] 是否有完整的錯誤處理？
- [ ] 是否記錄了API調用順序？
- [ ] 是否優先顯示中文名稱？
- [ ] 是否遵循API資料完整性規則？

### 測試驗證項目
- [ ] 新增股票時優先顯示中文名稱
- [ ] FinMind失敗時能正確切換到備用API
- [ ] Console日誌清楚顯示API調用過程
- [ ] 不同類型股票（股票、ETF、債券）都能正確處理

## 💡 最佳實踐

### API整合原則
1. **優先本土化**: FinMind專為台股設計，最適合台灣用戶
2. **中文優先**: 優先使用能提供中文名稱的API
3. **漸進降級**: API失敗時有序降級到備用方案
4. **完整日誌**: 記錄每個API的嘗試結果

### 性能優化
- 使用適當的快取機制
- 設定合理的超時時間
- 批次處理時控制並發數量
- 監控API回應時間和成功率

## 🎯 目標效果

### 用戶體驗
- **中文名稱優先**: 台股顯示中文名稱，提升本土用戶體驗
- **資料準確性**: FinMind專業台股資料，確保準確性
- **穩定可靠**: 多層備用機制，確保服務穩定

### 技術指標
- **FinMind成功率**: 目標 >90%
- **整體API成功率**: 目標 >99%
- **中文名稱覆蓋率**: 目標 >95%
- **API回應時間**: 目標 <2秒

---

**記住：FinMind API是台股資料的最佳選擇，必須作為第一優先！**